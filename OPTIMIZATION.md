# 🚀 Инструкция по оптимизации сайта

## ✅ Что уже сделано

### 1. GitHub Actions Workflow
**Файл**: `.github/workflows/optimize-and-deploy.yml`

**Возможности**:
- ✅ Автоматическая минификация JS/CSS/HTML
- ✅ Конвертация изображений в WebP
- ✅ Генерация Service Worker
- ✅ Автоматический деплой на GitHub Pages
- ✅ Отчет об экономии размера файлов

**Запуск**: Автоматически при каждом push в main

---

### 2. Lazy Loading для изображений
**Файл**: `js/lazy-loading.js`

**Возможности**:
- ✅ Автоматическая загрузка картинок при появлении в области видимости
- ✅ Placeholder с blur эффектом
- ✅ Поддержка srcset для responsive изображений
- ✅ Fallback для старых браузеров

**Как использовать**:
```html
<!-- Вместо: -->
<img src="/images/photo.jpg" alt="Photo">

<!-- Используйте: -->
<img data-src="/images/photo.jpg" alt="Photo" width="800" height="600">

<!-- С responsive: -->
<img data-src="/images/photo.jpg" 
     data-srcset="/images/photo-400.jpg 400w, /images/photo-800.jpg 800w"
     alt="Photo" 
     width="800" 
     height="600">
```

---

### 3. Service Worker
**Файлы**: 
- `js/sw-register.js` - регистрация
- `sw.js` - генерируется GitHub Actions

**Возможности**:
- ✅ Офлайн-режим
- ✅ Кэширование статических ресурсов
- ✅ Автоматическое обновление
- ✅ Уведомления о новой версии

---

### 4. Модульная структура
**Файл**: `js/modules/calculator-core.js`

**Преимущества**:
- ✅ Разделение кода по модулям
- ✅ Легче поддерживать
- ✅ Можно загружать по требованию (lazy load)

---

## 🛠️ Как внедрить

### Шаг 1: Обновить HTML

Добавьте в `<head>` каждой страницы:

```html
<!-- Preload критичных ресурсов -->
<link rel="preload" href="/css/variables.css" as="style">
<link rel="preload" href="/css/header.css" as="style">
<link rel="preload" href="/js/lazy-loading.js" as="script">
```

Добавьте перед `</body>`:

```html
<!-- Оптимизация -->
<script src="/js/lazy-loading.js"></script>
<script src="/js/sw-register.js"></script>
```

---

### Шаг 2: Обновить изображения

Замените все `<img src=...>` на `<img data-src=...>`:

```bash
# Автоматическая замена (мак OS/Linux)
find . -name "*.html" -type f -exec sed -i '' 's/<img src="/<img data-src="/g' {} +
```

Или вручную в важных местах (калькулятор, галерея).

---

### Шаг 3: Настройка GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. В **Source** выберите **GitHub Actions**
3. Workflow запустится автоматически

---

### Шаг 4: Проверка работы

1. **GitHub Actions**:
   - Перейдите во вкладку **Actions**
   - Проверьте логи сборки
   - Увидите отчет об экономии

2. **Lazy Loading**:
   - Откройте DevTools → Network
   - Перезагрузите страницу
   - Картинки должны загружаться по мере прокрутки

3. **Service Worker**:
   - Откройте DevTools → Application → Service Workers
   - Должен быть статус "activated and is running"
   - Проверьте офлайн-режим (чекбокс "Offline")

---

## 📊 Ожидаемые результаты

### До оптимизации:
- 🔴 JavaScript: ~30-50 KB
- 🔴 CSS: ~20-30 KB
- 🔴 Images: 2-5 MB
- 🔴 Загрузка: 2-3 секунды
- 🔴 Lighthouse Score: 60-70

### После оптимизации:
- 🟢 JavaScript: ~10-15 KB (↓ 60-70%)
- 🟢 CSS: ~8-12 KB (↓ 50-60%)
- 🟢 Images: 300-800 KB (↓ 85-90% с WebP)
- 🟢 Загрузка: 0.5-0.8 секунды (↓ 70-75%)
- 🟢 Lighthouse Score: 90-95 (↑ 30-40%)

---

## 🔧 Дополнительная оптимизация

### Critical CSS
Вынесите критичные стили inline:

```html
<style>
  /* Critical CSS для первого экрана */
  body { margin: 0; font-family: system-ui; }
  .header { /* ... */ }
</style>
```

### Resource Hints
Добавьте подсказки браузеру:

```html
<!-- DNS Prefetch -->
<link rel="dns-prefetch" href="//cdnjs.cloudflare.com">

<!-- Preconnect -->
<link rel="preconnect" href="https://fonts.googleapis.com">

<!-- Prefetch -->
<link rel="prefetch" href="/calculator.html">
```

### Code Splitting
Загружайте конфигуратор АМК только при необходимости:

```javascript
if (material.hasAMK) {
  import('./modules/amk-configurator.js')
    .then(module => module.init());
}
```

---

## ❓ FAQ

**Q: Почему GitHub Actions не запускается?**

A: Проверьте:
1. Settings → Actions → General → "Allow all actions"
2. Settings → Pages → Source = "GitHub Actions"

**Q: Lazy loading не работает в Safari?**

A: Safari 15.4+ поддерживает IntersectionObserver. Для старых версий есть fallback.

**Q: Как отключить Service Worker?**

A: Уберите `<script src="/js/sw-register.js"></script>` из HTML.

**Q: Можно ли не использовать GitHub Actions?**

A: Да, можно минифицировать локально и заливать готовые файлы.

---

## 👍 Рекомендации

1. ✅ **Начните с Lazy Loading** - наибольший эффект
2. ✅ **Затем Service Worker** - офлайн + кэширование
3. ✅ **GitHub Actions** - автоматизация всего
4. ✅ **Модули** - для больших проектов

---

🎉 **Все готово к использованию!**